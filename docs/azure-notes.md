# Notas Azure

Os arquivos de `Azure` continuam no repositorio apenas como referencia opcional de uma evolucao enterprise.

Eles nao fazem parte do caminho principal do projeto e nao sao necessarios para atender o PDF.

Pastas relacionadas:

- `infra/`
- `orchestration/adf/`
- `databricks/notebooks/`

Se a entrega for avaliada apenas pelo escopo do case, o fluxo principal a considerar e:

- `Google Colab`
- `PySpark`
- `Streamlit`
